devices:
  MPS:
    train_batch_size_sweep: [4]
    eval_batch_size_sweep: [1]
train_benchmark: true
train_deterministic: false
not_implemented:
  # Disabled due to excessively slow runtime - see GH Issue #100
  - test: train
    device: cpu
