# This is the configuration file that tunes the benchmark score v1
#
# Config version
CONFIG_VER=v1
# Python and CUDA Version
PYTHON_VERSION=3.7
CUDA_VERSION=cu102
# Number of iterations
NUM_ITER=1
# Number of rounds (minimum times to run each benchmark)
NUM_ROUNDS=20
# The CPU cores that run the benchmark
# On the g4dn.metal machine, limit the CPU cores to single NUMA node
CORE_LIST=24-47
# The GPU devices that run the benchmark
GPU_LIST=0
# The <memory, graphics> clock frequency setting of GPU
GPU_FREQUENCY=5001,900
# Benchmark filter generated by the stability analysis
BENCHMARK_FILTER="(not ((test_worker) or (train and alexnet and mps and eager) or (train and dcgan and mps and eager) or (train and hf_Bert and mps and eager) or (train and mnasnet1_0 and mps and eager) or (train and mobilenet_v2 and mps and eager) or (train and pytorch_unet and mps and eager) or (train and resnet18 and mps and eager) or (train and resnet50 and mps and eager) or (train and resnext50_32x4d and mps and eager) or (train and shufflenet_v2_x1_0 and mps and eager) or (train and timm_efficientnet and mps and eager) or (train and timm_nfnet and mps and eager) or (train and timm_regnet and mps and eager) or (train and timm_resnest and mps and eager) or (train and timm_vision_transformer and mps and eager) or (train and timm_vovnet and mps and eager) or (train and soft_actor_critic and mps and eager) or (train and hf_DistilBert and mps and eager) or (train and hf_Bart and mps and eager) or (train and hf_Albert and mps and eager) or (train and hf_GPT2 and mps and eager) or (train and lennard_jones and mps and eager) or (train and pytorch_stargan and mps and eager) or (train and pytorch_struct and mps and eager) or (train and timm_vision_transformer_large and mps and eager) or (train and functorch_dp_cifar10 and mps and eager) or (train and squeezenet1_1 and mps and eager) or (train and hf_T5_base and mps and eager) or (train and hf_T5_large and mps and eager) or (train and densenet121 and mps and eager) or (train and phlippe_resnet and mps and eager) or (train and phlippe_densenet and mps and eager) or (train and tts_angular and mps and eager) or (train and DALLE2_pytorch and mps and eager) or (train and functorch_maml_omniglot and mps and eager) or (train and demucs and mps and eager) or (train and vgg16 and mps and eager) or (eval and alexnet and mps and eager) or (eval and dcgan and mps and eager) or (eval and hf_Bert and mps and eager) or (eval and mnasnet1_0 and mps and eager) or (eval and mobilenet_v2 and mps and eager) or (eval and pytorch_unet and mps and eager) or (eval and resnet18 and mps and eager) or (eval and resnet50 and mps and eager) or (eval and resnext50_32x4d and mps and eager) or (eval and shufflenet_v2_x1_0 and mps and eager) or (eval and timm_efficientnet and mps and eager) or (eval and timm_nfnet and mps and eager) or (eval and timm_regnet and mps and eager) or (eval and timm_resnest and mps and eager) or (eval and timm_vision_transformer and mps and eager) or (eval and timm_vovnet and mps and eager) or (eval and soft_actor_critic and mps and eager) or (eval and hf_DistilBert and mps and eager) or (eval and hf_Bart and mps and eager) or (eval and hf_Albert and mps and eager) or (eval and hf_GPT2 and mps and eager) or (eval and lennard_jones and mps and eager) or (eval and pytorch_stargan and mps and eager) or (eval and pytorch_struct and mps and eager) or (eval and timm_vision_transformer_large and mps and eager) or (eval and functorch_dp_cifar10 and mps and eager) or (eval and squeezenet1_1 and mps and eager) or (eval and hf_T5_base and mps and eager) or (eval and hf_T5_large and mps and eager) or (eval and densenet121 and mps and eager) or (eval and phlippe_resnet and mps and eager) or (eval and phlippe_densenet and mps and eager) or (eval and tts_angular and mps and eager) or (eval and DALLE2_pytorch and mps and eager) or (eval and functorch_maml_omniglot and mps and eager) or (eval and demucs and mps and eager) or (eval and vgg16 and mps and eager)))"
# Config file
CONFIG_FILE=config-v1.yaml
